# Instructions for Codex

- Before starting work, run the build and start the dev server.
- After each change, run the build and start the dev server again.
- If starting the dev server fails due to the port being in use, kill the process using that port and try again.
